import "package:flutter/material.dart";

// Receive/income now uses the former expense color, and send/expense uses the former income color.
const receivedSemanticColor = Color(0xFFB02A37);
const sentSemanticColor = Color(0xFF198754);
